# AiGateway Master Key (GUID - used for signing and internal security)
# Generate a new one with: powershell -Command "[guid]::NewGuid()"
AIGATEWAY_MASTER_KEY=12345678-1234-1234-1234-123456789012

# Container User ID and Group ID
# IMPORTANT: These control permissions for volume mounts (./data and ./logs)
# Leave empty (default 1000) OR set to your host user:
#   Linux: AIGATEWAY_UID=$(id -u) AIGATEWAY_GID=$(id -g)
#   MacOS: AIGATEWAY_UID=$(id -u) AIGATEWAY_GID=$(id -g)
#   Windows (WSL2): AIGATEWAY_UID=$(id -u) AIGATEWAY_GID=$(id -g)
# Default 1000 usually works, but set to match your user if you get permission errors
AIGATEWAY_UID=1000
AIGATEWAY_GID=1000

# Upstream Services (Speaches and Ollama)
SPEACHES_BASE_URL=http://10.64.10.4:8000
OLLAMA_BASE_URL=http://10.64.10.5:11434

# Ollama Configuration (optional)
OLLAMA_DEFAULT_MODEL=llama3.2
OLLAMA_TIMEOUT=120
